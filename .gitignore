node_modules
.DS_Store

# Build artifacts
*.tgz

# In-place scan artifacts at repo root (samples/** is tracked explicitly)
/apigate-report.json
/*.html

# Agent tooling state — never commit
/.a5c/

# Test fixture scan artifacts (regenerated on each run)
test/fixtures/**/apigate-report.json
test/fixtures/**/*.html

# Local agent state
/.claude/
/.a5c/logs/
/.a5c/cache/
